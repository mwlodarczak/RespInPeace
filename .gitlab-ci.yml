image: alpine

pages:
  script:
  - apk --no-cache add py3-pip python3-dev python3-numpy python3-scipy python3-pandas python3-sphinx
  - pip3 install tgt
  - apk --no-cache add make
  - cd doc
  - make html
  - mv build/html/ ../public/
  artifacts:
    paths:
    - public
  only:
  - master
